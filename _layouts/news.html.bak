---
layout: default
---

<h1>Yeni yazılar</h1>

{% for post in site.posts %}
    <a href="{{post.url}}"><h2>{{post.title}}</h2></a>
    <p>{{ post.content | strip_html | truncate: 50 }}</p>
{% endfor %}